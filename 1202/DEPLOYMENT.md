# GitHub Pages 배포 가이드

이 문서는 GitHub Pages에 배포하는 두 가지 방법을 설명합니다:
- **자동 배포**: GitHub Actions를 사용한 자동 배포
- **수동 배포**: 명령어를 직접 실행하는 수동 배포

---

## 🚀 자동 배포 설정 (권장)

GitHub에 코드를 푸시할 때마다 자동으로 빌드하고 배포하는 방법입니다.

### 1. GitHub Actions 워크플로우 파일 생성

`.github/workflows/deploy.yml` 파일을 생성하고 다음 내용을 추가하세요:

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main  # 또는 master (기본 브랜치 이름에 맞게 수정)

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    permissions:
      contents: read
      pages: write
      id-token: write
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Build
        run: npm run build
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
      
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
```

### 2. GitHub 저장소 설정

1. GitHub 저장소로 이동
2. **Settings** → **Pages** 메뉴로 이동
3. **Source**를 "GitHub Actions"로 변경

### 3. 환경 변수 설정 (선택사항)

만약 빌드 시 `GEMINI_API_KEY`가 필요하다면:

1. GitHub 저장소 → **Settings** → **Secrets and variables** → **Actions**
2. **New repository secret** 클릭
3. Name: `GEMINI_API_KEY`
4. Value: 실제 API 키 값 입력
5. **Add secret** 클릭

### 4. 사용 방법

이제 `main` 브랜치에 코드를 푸시하면 자동으로 빌드하고 배포됩니다:

```bash
git add .
git commit -m "Update"
git push origin main
```

### 자동 배포의 장점

- ✅ 코드 푸시만으로 자동 배포
- ✅ 실수로 배포를 깜빡할 일이 없음
- ✅ 배포 히스토리를 GitHub Actions에서 확인 가능
- ✅ 빌드 오류를 미리 확인 가능

---

## 📝 수동 배포 방법

명령어를 직접 실행하여 배포하는 방법입니다.

### 빠른 배포 방법

터미널에서 다음 명령어만 실행하면 됩니다:

```bash
npm run deploy
```

이 명령어는 자동으로:
1. `predeploy` 스크립트 실행 → `npm run build` (빌드)
2. `deploy` 스크립트 실행 → `gh-pages -d dist` (GitHub Pages에 배포)

### 단계별 실행 방법 (선택사항)

원한다면 단계별로 실행할 수도 있습니다:

```bash
# 1. 빌드만 실행
npm run build

# 2. 배포만 실행
npx gh-pages -d dist
```

### 배포 전 확인사항

#### 1. 변경사항 커밋 및 푸시

배포하기 전에 변경사항을 GitHub에 푸시했는지 확인하세요:

```bash
git add .
git commit -m "Update"
git push origin main
```

#### 2. 환경 변수 확인

빌드 시 필요한 환경 변수가 설정되어 있는지 확인하세요:
- `.env.local` 파일에 `GEMINI_API_KEY`가 설정되어 있는지 확인

### 배포 후 확인

배포가 완료되면 (보통 1-2분 소요):

1. **GitHub Pages URL 확인**
   - `https://github.com/bhher/pension/` (또는 설정한 GitHub Pages URL)
   - 실제 배포된 사이트가 정상적으로 작동하는지 확인

2. **GitHub 저장소에서 확인**
   - GitHub 저장소 → **Settings** → **Pages** 메뉴
   - 배포 상태 및 URL 확인

3. **gh-pages 브랜치 확인**
   - GitHub 저장소에서 `gh-pages` 브랜치가 생성되었는지 확인
   - 이 브랜치에 배포된 파일들이 있습니다

### 수동 배포의 장점

- ✅ 배포 시점을 직접 제어 가능
- ✅ 배포 전 로컬에서 미리 테스트 가능
- ✅ 추가 설정 없이 바로 사용 가능

---

## ⚠️ 주의사항

### 공통 주의사항

- 배포는 GitHub 저장소에 푸시 권한이 있어야 합니다
- 첫 배포 후 GitHub 저장소의 **Settings** → **Pages**에서 소스를 올바르게 설정해야 합니다
- 배포는 보통 1-2분 정도 소요됩니다

### 자동 배포 주의사항

- 기본 브랜치가 `master`인 경우, 워크플로우 파일의 `branches: - main`을 `branches: - master`로 변경하세요
- 배포 상태는 **Actions** 탭에서 확인할 수 있습니다

### 수동 배포 주의사항

- `npm run deploy` 실행 시 `dist` 폴더의 내용이 `gh-pages` 브랜치에 자동으로 푸시됩니다
- 배포 전에 로컬에서 `npm run build`로 빌드가 정상적으로 되는지 확인하는 것을 권장합니다

---

## 🔧 문제 해결

### 배포가 실패하는 경우

1. **Git 인증 확인**
   ```bash
   git config --global user.name "Your Name"
   git config --global user.email "your.email@example.com"
   ```

2. **GitHub Pages 설정 확인**
   - Settings → Pages → Source가 올바르게 설정되어 있는지 확인
   - 자동 배포: "GitHub Actions"
   - 수동 배포: "gh-pages" 브랜치

3. **빌드 오류 확인**
   ```bash
   npm run build
   ```
   - 빌드가 정상적으로 완료되는지 먼저 확인

### 배포는 되었지만 사이트가 보이지 않는 경우

- GitHub Pages 설정에서 브랜치가 올바르게 선택되었는지 확인
- 배포 완료 후 몇 분 정도 기다려보세요 (캐시 문제일 수 있음)
- 브라우저 캐시를 지우고 다시 시도해보세요
- `vite.config.ts`의 `base: "pension/"` 설정이 올바른지 확인

---

## 📚 참고 자료

- [GitHub Pages 공식 문서](https://docs.github.com/en/pages)
- [GitHub Actions 공식 문서](https://docs.github.com/en/actions)
- [gh-pages 패키지 문서](https://github.com/tschaub/gh-pages)

